{
  "name": "friggo-app",
  "version": "1.0.0",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "cd client && npm install && npm run build && cd ../server && npm install && npm run build && npx prisma generate",
    "start": "cd server && npx prisma migrate deploy && cd .. && node server/dist/index.js"
  }
}
