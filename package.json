{
  "name": "friggo-app",
  "version": "1.0.0",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "cd server && npm install && npm run build",
    "start": "node server/dist/index.js"
  }
}
